name: api_documentos

on:
  push:
    branches: [ "main", "principal" ]
  pull_request:
    branches: [ "main", "principal" ]

jobs:
  build-test-quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        [cite_start]uses: actions/checkout@v4 [cite: 74, 277]

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          [cite_start]node-version: 20 [cite: 75, 279]

      - name: Instalar dependencias
        [cite_start]run: npm ci [cite: 79, 280]

      - name: Lint Check (Gate 1)
        [cite_start]run: npm run lint [cite: 32, 281] # Falla si hay errores de estilo

      - name: Ejecutar Tests y Cobertura (Gate 2)
        [cite_start]run: npm test -- --coverage [cite: 62, 80] # Debe ser >= 80% [cite: 32]

      - name: SAST Scan con CodeQL (Gate 3)
        uses: github/codeql-action/analyze@v3
        with:
          [cite_start]languages: javascript [cite: 52, 84] # Falla si hay vulnerabilidades críticas [cite: 63]

  package-and-scan:
    needs: build-test-quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Docker Image (Multi-stage)
        [cite_start]run: docker build -t api-documentos:${{ github.sha }} . [cite: 64, 87]

      - name: Scan CVE con Trivy (Gate 4)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'api-documentos:${{ github.sha }}'
          exit-code: 1
          [cite_start]severity: 'CRITICAL,HIGH' [cite: 33, 88] # Falla si hay CVE críticos

  deploy-and-smoke-test:
    needs: package-and-scan
    runs-on: ubuntu-latest
    steps:
      - name: Simular Deploy a Staging
        [cite_start]run: echo "Desplegando incremento funcional..." [cite: 67, 90]

      - name: Smoke Test (Salud del sistema)
        run: |
          # En un entorno real aquí se usaría la URL de staging
          echo "Verificando endpoint /health..." [cite: 67, 93, 117]
